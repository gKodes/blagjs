export class PromptHandler {
  /**
   *  Render the mode using the prompt handler
   *
   * @param {Object|string} model
   * @returns {string} an unique id for the rendered model which will help identify the response
   */
  render(model) {
    // Render the model
    // Return a promit id
  }

  // response handler
}
